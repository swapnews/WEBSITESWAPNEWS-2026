// CSS shell dan komponen bersama dashboard. Stylesheet berat editor,
// media, SEO, dan merchandise dimuat oleh nested layout masing-masing.
import "@/styles/dashboard-mint.css";

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
    return children;
}
